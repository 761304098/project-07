<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sun.dao.zhubanMapper">
      <select id="queryForAll" resultType="zhuban">
           select  * from zhuban order by price
      </select>
     <select id="queryForName" resultType="zhuban" parameterType="String">
             select  * from zhuban where name like  concat('%',#{brand},'%')  order by price
     </select>
    <select id="queryForPrice1" resultType="zhuban" >
        select * from zhuban  where price&lt;400 order by price
    </select>
    <select id="queryForPrice2" resultType="zhuban" >
        select * from zhuban  where price&gt;=400 and price&lt;800  order by price
    </select>
    <select id="queryForPrice3" resultType="zhuban" >
        select * from zhuban  where price&gt;=800  order by price
    </select>
    <select id="queryForGroup" resultType="zhuban" parameterType="String" >
        select * from zhuban  where group1 like  concat('%',#{group},'%') order by price
    </select>

    <select id="queryForSlot" resultType="zhuban" parameterType="String" >
        select * from zhuban  where  cpuslot like  concat('%',#{slot},'%') order by price
    </select>
    <select id="queryForType" resultType="zhuban" parameterType="String" >
        select * from zhuban  where  type like  concat('%',#{type},'%') order by price
    </select>
    <select id="queryForChip" resultType="zhuban" parameterType="String" >
        select * from zhuban  where chip=#{chip}
    </select>
</mapper>